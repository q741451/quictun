bazel-*

# Regenerated fresh before every build by
# build/regen_quictun_build_timestamp.sh -- never checked in.
/quiche/quic/tools/quictun_build_timestamp.h

# Generated by build/toolchain/setup_toolchain.sh -- the from-source musl
# toolchain and the absolute paths BUILD.bazel loads from it. Never checked in.
/build/toolchain/out/
/build/toolchain/toolchain_paths.bzl
